services:
  remote-ops-web:
    build:
      context: ..
      dockerfile: docker/Dockerfile.web
    ports:
      - "8765:8765"
    environment:
      ROW_HOME: /data
    volumes:
      - remote-ops-data:/data
volumes:
  remote-ops-data:
